---
import MinimalLayout from "../layouts/MinimalLayout.astro";
---

<MinimalLayout
  title="404 | The Ride"
  description="Page not found - Get back on track with Ireland's cycling data"
>
  <section class="bg-velvet-black min-h-screen flex items-center justify-center px-4 py-12 md:py-20">
    <div class="container mx-auto max-w-3xl text-center">

      <!-- Massive 404 Number -->
      <div class="mb-8">
        <h1 class="text-8xl md:text-[12rem] font-bold text-neon-yellow neon-glow-yellow-strong tracking-tight leading-none">
          404
        </h1>
      </div>

      <!-- Headline -->
      <h2 class="text-2xl md:text-4xl font-medium text-white mb-6">
        Ah here.
      </h2>

      <!-- Body Copy -->
      <p class="text-base md:text-xl text-text-secondary mb-10 max-w-xl mx-auto">
        This page is as missing as a bike lane in traffic
      </p>

      <!-- Primary CTA -->
      <a href="/" class="btn-primary inline-block text-lg font-medium mb-12 px-8 py-4">
        Get back on track
      </a>

      <!-- Neon Divider -->
      <div class="neon-divider my-12 max-w-md mx-auto"></div>

    </div>
  </section>
</MinimalLayout>

<style>
  /* Custom cyan navigation link style */
  .nav-link-cyan {
    @apply bg-transparent text-white border-2 border-neon-cyan rounded-lg px-6 py-3
           transition-all duration-300 hover:bg-neon-cyan hover:text-velvet-black
           inline-block;
  }
  .nav-link-cyan:hover {
    box-shadow: var(--glow-cyan);
    transform: scale(1.02);
  }

  /* Ensure 404 number has extra strong glow */
  .neon-glow-yellow-strong {
    text-shadow: 0 0 10px rgba(204, 255, 0, 0.8),
                 0 0 20px rgba(204, 255, 0, 0.6),
                 0 0 40px rgba(204, 255, 0, 0.4),
                 0 0 60px rgba(204, 255, 0, 0.3);
  }

  /* Ensure full viewport height */
  section {
    min-height: 100vh;
  }
</style>
