{
  "name": "update-sensor-data",
  "version": "1.0.0",
  "description": "Scheduled worker to update sensor data from Telraam API",
  "main": "index.ts",
  "scripts": {
    "build": "echo 'No build step needed - wrangler handles compilation'",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0"
  }
}
