{
  "name": "scrape-location-names",
  "version": "1.0.0",
  "description": "Scheduled worker to scrape location names from Telraam pages",
  "main": "index.ts",
  "scripts": {
    "build": "echo 'No build step needed - wrangler handles compilation'",
    "dev": "wrangler dev -c wrangler.toml",
    "deploy": "wrangler deploy -c wrangler.toml",
    "tail": "wrangler tail scrape-location-names"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0"
  }
}
